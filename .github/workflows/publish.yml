name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:   # re-publish the current main (e.g. after setting up Trusted Publishing)

# Trusted Publishing: no API token stored anywhere. Link this workflow once on
# PyPI (project "subcortex" -> Publishing -> GitHub: pavlealeksic/subcortex,
# workflow "publish.yml", environment "pypi").
permissions:
  id-token: write
  contents: read   # needed to check out private repos

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: python -m pip install -e . pyyaml   # hooks run as `python -I -m subcortex.hook`
      - name: Unit tests
        run: python -m unittest discover -s tests
      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
